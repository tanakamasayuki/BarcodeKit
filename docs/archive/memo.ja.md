# BarcodeKit 要件仕様書

## 1. 概要

BarcodeKitは、Arduinoをはじめとする組み込み環境向けに、各種1次元バーコードおよび2次元コードを生成するためのライブラリである。

本ライブラリは、文字列や数値などの入力データを、バーコードを構成する論理的な白黒パターンへ変換することを主目的とする。

液晶、OLED、電子ペーパー、プリンターなどへの具体的な描画処理はライブラリの中心機能には含めず、利用例として提供する。

---

## 2. 目的

BarcodeKitの目的は、複数種類のバーコードを、組み込み環境で統一的かつ簡単に生成できる仕組みを提供することである。

主な目的は以下のとおりとする。

* 複数のバーコード形式を共通の考え方で扱えるようにする
* 表示デバイスや描画ライブラリに依存しない構成とする
* Arduinoなどの限られたメモリ環境でも利用可能にする
* バーコード生成に必要な仕様上の処理をライブラリ側で完結させる
* 利用者が生成された白黒パターンを自由な方法で描画、印刷、転送できるようにする
* 必要なバーコード形式だけを選択しやすい構成とする
* 将来的な対応形式の追加を妨げない設計方針とする

---

## 3. 対象環境

BarcodeKitは、主として以下の環境での利用を想定する。

* Arduino互換環境
* AVR系マイコン
* ESP32系マイコン
* RP2040系マイコン
* SAMD系マイコン
* その他、Arduino互換APIまたはC++を利用可能な組み込み環境

特定の表示デバイス、ディスプレイライブラリ、通信方式には依存しないものとする。

---

## 4. 対象利用者

主な対象利用者は以下とする。

* Arduinoでバーコードを表示したい開発者
* OLEDやTFT液晶にQRコードを表示したい開発者
* 電子ペーパーにJANコードやCode 128を表示したい開発者
* 小型プリンターへバーコードパターンを出力したい開発者
* 複数のバーコード形式を共通のライブラリで扱いたい開発者
* 表示処理とバーコード生成処理を分離したい開発者

---

## 5. 適用範囲

BarcodeKitが担当する範囲は、入力データをバーコードの論理パターンへ変換するまでとする。

ライブラリの責務には以下を含む。

* 入力データの受付
* 入力値の妥当性確認
* バーコード形式ごとの符号化
* 必要なチェックディジットの計算
* 誤り訂正情報の生成
* バーコードを構成する白黒モジュールの生成
* 出力サイズに関する情報の提供
* 推奨クワイエットゾーンに関する情報の提供
* エラー内容の通知

以下はライブラリの中心的な責務には含めない。

* 特定ディスプレイへの描画
* 特定プリンターへの印刷
* 画像ファイルの生成
* カメラ画像からのバーコード読み取り
* バーコードのデコード
* 画像補正や画像認識
* 通信機器固有の出力制御
* GUIの提供

---

## 6. 基本方針

### 6.1 生成専用

BarcodeKitは、バーコードの生成に特化したライブラリとする。

バーコードの読み取りやデコードは対象外とする。

バーコード読み取りについては、一般的に専用スキャナやカメラ画像認識が利用されるため、本ライブラリでは対応しない。

### 6.2 描画処理との分離

バーコードの符号化処理と、画面や紙への描画処理を明確に分離する。

BarcodeKitは、生成されたバーコードの各位置が黒か白かを判定できる情報を提供する。

利用者は、その情報を用いて任意の描画ライブラリや出力装置へ表示する。

### 6.3 デバイス非依存

BarcodeKit本体は、以下のような外部描画環境に直接依存しない。

* Adafruit GFX
* TFT_eSPI
* U8g2
* LovyanGFX
* 電子ペーパー用ライブラリ
* サーマルプリンター用ライブラリ

これらとの連携方法は、サンプルまたは補助的な文書として提供する。

### 6.4 軽量性

組み込み環境での使用を前提とし、メモリ使用量とプログラムサイズを抑えることを重視する。

特に小容量RAMを持つマイコンでも、対応可能なバーコード形式については利用できることを目標とする。

### 6.5 統一性

バーコード形式ごとに完全に異なる利用方法を要求せず、可能な範囲で共通した操作方法を提供する。

ただし、各バーコード形式固有の設定や制約は適切に表現できるものとする。

---

## 7. 対応バーコード形式

初期段階では、以下の形式を主な候補とする。

### 7.1 1次元バーコード

* Code 39
* Code 93
* Code 128
* EAN-8
* EAN-13
* UPC-A
* UPC-E
* ITF
* ITF-14
* Codabar

### 7.2 2次元コード

* QR Code

対応形式は段階的に追加できるものとする。

以下の形式は将来的な拡張候補とする。

* Data Matrix
* PDF417
* Aztec Code
* GS1 DataMatrix
* GS1 QR Code

すべての形式を初期リリースで対応することは必須としない。

---

## 8. 機能要件

### 8.1 バーコード形式の選択

利用者は、生成対象となるバーコード形式を明示的に指定できること。

指定された形式に対応していない場合は、適切なエラーを返すこと。

### 8.2 入力データの指定

利用者は、文字列またはバイト列として入力データを指定できること。

バーコード形式によって使用可能な文字種や文字数が異なるため、各形式の規則に従って入力を検証すること。

### 8.3 入力値の検証

以下のような入力不備を検出できること。

* 使用できない文字を含む
* 桁数が不足している
* 桁数が上限を超えている
* 数字専用形式に数字以外が指定されている
* チェックディジットが正しくない
* QRコードの容量を超えている
* 指定された設定の組み合わせが不正である

### 8.4 チェックディジット

チェックディジットを必要とする形式について、以下の扱いを可能とする。

* ライブラリによる自動計算
* 入力済みチェックディジットの検証
* 形式上必要な場合の自動付加

チェックディジットの扱いは、形式ごとの仕様に従う。

### 8.5 QRコード設定

QRコードでは、少なくとも以下の設定を扱えること。

* 誤り訂正レベル
* バージョンの自動選択
* 必要に応じたバージョン指定
* 入力データ量に応じた容量判定

### 8.6 論理パターンの提供

生成結果として、バーコードを構成する白黒モジュールの状態を取得できること。

1次元バーコードでは、横方向に並ぶバーとスペースの情報を取得できること。

2次元コードでは、縦横に並ぶ各セルの白黒状態を取得できること。

### 8.7 サイズ情報

生成結果から、少なくとも以下の情報を取得できること。

* 論理上の幅
* 論理上の高さ
* バーコードの種類
* 推奨クワイエットゾーン
* 生成に使用された設定
* 必要に応じてチェックディジットを含む最終データ

ここでいう幅と高さは、画素数ではなく、バーコードを構成する最小単位の数とする。

### 8.8 クワイエットゾーン

各バーコード形式について、推奨される余白情報を提供できること。

クワイエットゾーン自体を出力パターンに含めるか、別情報として提供するかは、一貫した方針を定めること。

利用者が余白を省略した場合、読み取り性能が低下する可能性があることを文書で明示する。

### 8.9 省メモリ利用

小容量RAM環境を考慮し、生成結果全体を保持する方法だけでなく、必要に応じて逐次的に出力を処理できる利用形態を検討する。

1次元バーコードについては、全体のビットマップを保持せずに処理できることが望ましい。

2次元コードについては、形式上必要となる作業メモリを明確にする。

### 8.10 エラー通知

エラー発生時に、原因を判別可能な情報を返すこと。

主なエラー区分は以下を想定する。

* 未対応形式
* 不正な入力文字
* 不正な入力長
* 容量超過
* バッファ不足
* 不正な設定
* チェックディジット不一致
* メモリ不足
* 内部生成失敗

---

## 9. 非機能要件

### 9.1 移植性

特定のマイコンアーキテクチャに強く依存しないこと。

一般的なArduino互換環境で利用可能であることを目標とする。

### 9.2 外部依存の最小化

ライブラリ本体の外部依存を可能な限り少なくする。

特定のグラフィックライブラリを必須依存にしない。

### 9.3 メモリ効率

動的メモリ確保への依存を避ける、または最小限にすることが望ましい。

利用者が事前に必要メモリ量を確認できることが望ましい。

### 9.4 安定性

不正な入力によって、範囲外アクセス、メモリ破壊、予期しない停止が発生しないこと。

### 9.5 再現性

同一の入力データ、バーコード形式および設定からは、常に同一の論理パターンが生成されること。

### 9.6 可読性

利用者向けの名称、設定項目、エラー内容は、バーコードに詳しくない開発者でも理解しやすいものとする。

### 9.7 拡張性

新しいバーコード形式を追加する際に、既存形式の利用方法を大きく変更しないこと。

---

## 10. 出力モデル

BarcodeKitの出力は、画像ではなく論理的な白黒パターンとする。

### 10.1 1次元バーコード

1次元バーコードは、横方向に並ぶ黒バーと白スペースの集合として表現する。

高さは描画時に利用者が決定する。

必要に応じて、バーの種類や高さが異なる形式にも対応可能な情報を提供する。

### 10.2 2次元コード

2次元コードは、縦横に配置された白黒セルの集合として表現する。

利用者は、各セルを任意の倍率で正方形として描画する。

### 10.3 表示倍率

表示倍率や実際の画素サイズはBarcodeKitでは決定しない。

利用者が出力先の解像度や物理サイズに応じて指定する。

バーコードの最小単位は整数倍率で描画することを推奨する。

---

## 11. 描画サンプル

描画機能はライブラリ本体の必須機能とはしないが、利用方法を示すためにサンプルを提供する。

サンプル候補は以下とする。

* シリアルモニターへの文字表示
* LovyanGFXを使用した表示
* TFT液晶への表示
* 電子ペーパーへの表示
* 1次元バーコードの描画例
* QRコードの描画例
* 画面中央への配置例
* クワイエットゾーンを含めた描画例
* 表示領域に収まる最大倍率の計算例

サンプルはBarcodeKit本体の依存関係を増やさない形で提供する。

---

## 12. デコード機能の扱い

BarcodeKitでは、バーコードのデコード機能を提供しない。

デコードには、一般的に以下の処理が必要となる。

* カメラまたは画像センサーからの画像取得
* 画像の明るさ補正
* 二値化
* 傾き補正
* バーコード領域の検出
* パターン解析
* 誤り訂正
* データ復元

これらは生成処理と比べて計算量とメモリ使用量が大きく、専用のバーコードスキャナや画像認識ライブラリを利用する方が適している。

そのため、BarcodeKitは生成機能に集中する。

---

## 13. 想定ユースケース

### 13.1 商品情報表示

電子ペーパーや液晶にJANコードを表示し、外部スキャナで読み取る。

### 13.2 デバイス設定

デバイス固有の設定URLや識別情報をQRコードとして表示する。

### 13.3 機器識別

機器番号やシリアル番号をCode 128またはCode 39として表示する。

### 13.4 発券・受付

整理番号や受付情報をバーコードとして小型プリンターへ出力する。

### 13.5 ネットワーク設定

Wi-Fi接続情報などをQRコードとして画面に表示する。

### 13.6 物流用途

箱番号、ロット番号、管理番号などをITFやCode 128として表示または印刷する。

---

## 14. 対象外事項

以下はBarcodeKitの対象外とする。

* バーコードスキャナ機能
* カメラ制御
* 画像解析
* バーコード画像からのデコード
* 読み取り成功率の保証
* 特定プリンターのドライバ
* 特定ディスプレイのドライバ
* ラベルレイアウト作成
* テキストやロゴを含む帳票生成
* GS1などの業務運用ルール全体の管理
* バーコード規格の認証取得
* 印刷品質の検査

---

## 15. ドキュメント要件

利用者向け文書には、少なくとも以下を含める。

* BarcodeKitの目的
* 対応バーコード形式
* 対応環境
* 基本的な利用手順
* 入力可能な文字と長さ
* チェックディジットの扱い
* QRコードの誤り訂正設定
* クワイエットゾーンの説明
* 出力サイズの考え方
* メモリ使用上の注意
* 描画倍率に関する注意
* 対応していない機能
* 各形式の制限事項
* エラー内容の説明

---

## 16. 品質目標

BarcodeKitは、以下の品質を目標とする。

* 規格に従った正しいバーコードを生成できる
* 一般的なバーコードスキャナで読み取り可能なパターンを生成できる
* 不正入力を適切に拒否できる
* 小容量マイコンでも利用方法を選択できる
* 表示デバイスに依存せず再利用できる
* 複数形式を一貫した考え方で扱える
* 新しい形式を継続的に追加できる

読み取り可否は、表示サイズ、解像度、コントラスト、余白、印刷品質、スキャナ性能などにも左右されるため、すべての環境での読み取り成功を保証するものではない。

---

## 17. 初期リリースの方針

初期リリースでは、対応形式を過度に増やすことよりも、基本構造と品質を優先する。

初期対応候補は以下とする。

* Code 39
* Code 128
* EAN-8
* EAN-13
* UPC-A
* ITF
* Codabar
* QR Code

初期リリースの目標は以下とする。

* 1次元バーコードとQRコードを共通ライブラリで扱える
* 外部描画ライブラリへ依存しない
* 入力検証とチェックディジット処理を提供する
* 白黒モジュールを取得できる
* Arduino向けの基本サンプルを提供する
* AVR、ESP32、RP2040のいずれか複数環境で利用できる

---

## 18. 将来拡張

将来的には以下を検討する。

* 対応バーコード形式の追加
* GS1形式への対応
* Data Matrixへの対応
* PDF417への対応
* Aztec Codeへの対応
* ストリーム形式の出力強化
* メモリ使用量を抑えた生成方式
* 描画補助モジュールの分離提供
* テスト用出力形式の追加
* 対応マイコンの拡大
* 各バーコード形式の適合性試験の充実

---

## 19. ライブラリの位置付け

BarcodeKitは、バーコード画像を直接作成するグラフィックライブラリではない。

また、バーコードを読み取るスキャナライブラリでもない。

BarcodeKitは、入力データをバーコード規格に従った論理的な白黒パターンへ変換する、組み込み向けのバーコード生成ライブラリである。

表示、印刷、保存、通信などの具体的な出力方法は利用者が選択できるものとする。
